# Starter persistence is PostgreSQL via Drizzle

The Bible listed both PostgreSQL and MySQL. The Starter’s documented path is PostgreSQL accessed through Drizzle: one default for Docker, seeds, and CI, with a narrow infrastructure adapter so a Product could swap the engine later. MySQL is not a Starter default.
