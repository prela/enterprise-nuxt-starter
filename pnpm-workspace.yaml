# Workspace packages are the Nuxt Layers. pnpm 11 also reads settings from this file.
# Only listed packages may run install scripts (pnpm 11 allowBuilds).
packages:
  - 'layers/*'
allowBuilds:
  esbuild: true
  vue-demi: true
